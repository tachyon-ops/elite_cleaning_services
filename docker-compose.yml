version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: elite-cleaning-frontend
    ports:
      - "127.0.0.1:3003:3000"
    volumes:
      - ./prisma/dev.db:/app/prisma/dev.db
    restart: always
